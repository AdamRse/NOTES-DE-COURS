//controller xbox One (repo https://github.com/medusalix/xone)
git clone https://github.com/medusalix/xone && cd xone && sudo ./install.sh
sudo xone-get-firmware.sh
    Pour installer le capteur bluetooth

//Lutris
Télécharger le .deb https://lutris.net/downloads
Installer avec
    sudo apt install./<.deb>
        Permet de télécharger les dépendances de paquets

//ckb-next pour driver souris corsair (https://github.com/ckb-next/ckb-next)
sudo apt install build-essential cmake libudev-dev qtbase5-dev zlib1g-dev libpulse-dev libquazip5-dev libqt5x11extras5-dev libxcb-screensaver0-dev libxcb-ewmh-dev libxcb1-dev qttools5-dev git libdbusmenu-qt5-dev
    Dépendances linux
git clone https://github.com/ckb-next/ckb-next.git && cd ckb-next
(sudo?) bash quickinstall

//nvm (https://github.com/nvm-sh/nvm)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
    on peut installer npm avec sudo apt install npm

//Ajout .bashrc
Pour éviter de poluer le dossier $HOME
    export XDG_CONFIG_HOME="$HOME/.config"
    export XDG_DATA_HOME="$HOME/.local/share"
    export XDG_CACHE_HOME="$HOME/.cache"
        Ne pas oublier de créer les répertoires associés
